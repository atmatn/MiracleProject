<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.youdao.crop.dao.Data_tree_itemsDao">

    <!--<select id="findById" resultType="Product" parameterType="String">-->
    <!--select * from product where productId=#{productId};-->
<!--</select>-->
    <!--<select id="findAll" resultType="Product">-->
        <!--select * from Product;-->
    <!--</select>-->
    <select id="findAll" resultType="Data_tree_items">
        select * from Productteam;
    </select>
    <!--<insert id="addProduct" parameterType="Product">-->
        <!--INSERT INTO product(pruductName,db_source) VALUES(#{dname},DATABASE());-->
    <!--</insert>-->

</mapper>